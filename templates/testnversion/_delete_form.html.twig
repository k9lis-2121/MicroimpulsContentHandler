<form method="post" action="{{ path('app_testnversion_delete', {'id': testnversion.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ testnversion.id) }}">
    <button class="btn">Delete</button>
</form>
