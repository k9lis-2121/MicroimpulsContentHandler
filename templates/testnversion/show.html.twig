{% extends 'base.html.twig' %}

{% block title %}Testnversion{% endblock %}

{% block body %}
    <h1>Testnversion</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ testnversion.id }}</td>
            </tr>
            <tr>
                <th>Title</th>
                <td>{{ testnversion.title }}</td>
            </tr>
            <tr>
                <th>Message</th>
                <td>{{ testnversion.message }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_testnversion_index') }}">back to list</a>

    <a href="{{ path('app_testnversion_edit', {'id': testnversion.id}) }}">edit</a>

    {{ include('testnversion/_delete_form.html.twig') }}
{% endblock %}
