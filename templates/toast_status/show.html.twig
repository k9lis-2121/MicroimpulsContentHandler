{% extends 'base.html.twig' %}

{% block title %}ToastStatus{% endblock %}

{% block body %}
    <h1>ToastStatus</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ toast_status.id }}</td>
            </tr>
            <tr>
                <th>Component</th>
                <td>{{ toast_status.component }}</td>
            </tr>
            <tr>
                <th>Title</th>
                <td>{{ toast_status.title }}</td>
            </tr>
            <tr>
                <th>Body</th>
                <td>{{ toast_status.body }}</td>
            </tr>
            <tr>
                <th>Viewed</th>
                <td>{{ toast_status.viewed ? 'Yes' : 'No' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_toast_status_index') }}">back to list</a>

    <a href="{{ path('app_toast_status_edit', {'id': toast_status.id}) }}">edit</a>

    {{ include('toast_status/_delete_form.html.twig') }}
{% endblock %}
