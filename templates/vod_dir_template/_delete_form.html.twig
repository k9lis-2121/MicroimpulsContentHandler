<form method="post" action="{{ path('app_vod_dir_template_delete', {'id': vod_dir_template.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ vod_dir_template.id) }}">
    <button class="btn">Delete</button>
</form>
